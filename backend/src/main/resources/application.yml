spring:
  application:
    name: panini-inventario
  datasource:
    url: jdbc:mariadb://localhost:3306/panini_db
    username: root
    password: 
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
